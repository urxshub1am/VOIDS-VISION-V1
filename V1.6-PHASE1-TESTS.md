# V1.6 Phase 1 validation

Local static validation passed before branch upload:

- all `js/*.js` files pass `node --check`
- relative ES module imports resolve
- no duplicate DOM IDs
- one-hand primary preference accepts `move`, `rotate`, `depth` and safely falls back to `move`
- one-hand ROTATE changes yaw/pitch without translating X/Y
- one-hand DEPTH changes Z without rotating
- mode switching rebases before applying movement
- Snap-aware primary MOVE quantizes to 0.25 world-unit steps
- a complete primary gesture remains one Undo/Redo transaction
- V1.5.4 free-space secondary manipulator, reacquisition, WebGL export, model-asset limit, secondary Transform Assist, and 64 MB persistence guards remain present

Real Chrome/Edge webcam + WebGL validation is still required before V1.6 can be considered stable.
